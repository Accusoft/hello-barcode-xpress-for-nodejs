# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# For this sample, we are ignoring package-lock.json to ensure you are always
# getting the latest available version of barcode-js allowed by
# package.json. In a real application, however, we recommend you do NOT ignore
# package-lock.json but check it into source control instead.
package-lock.json

# dependencies
/node_modules

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
